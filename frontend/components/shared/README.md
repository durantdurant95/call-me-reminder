# Shared Components

This folder contains reusable components that are used across multiple features:

- **empty-state.tsx**: (To be created) Empty state component with icon, title, description
- **status-badge.tsx**: (To be created) Status indicator badges for reminders
- **countdown-timer.tsx**: (To be created) Live countdown timer for scheduled reminders
- **loading-state.tsx**: (To be created) Skeleton loaders and loading indicators

These components are generic and can be reused throughout the application.
